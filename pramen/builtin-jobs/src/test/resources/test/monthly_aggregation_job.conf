pramen.output.info.date.expr = "@infoDate - 2"

pramen.aggregation {
  factory.class = za.co.absa.pramen.mocks.DummyAggregationJob
  job.name = "Job Name"

  schedule.type = "monthly"
  schedule.days.of.month = [ 2 ]

  input.tables = [ table1, table2 ]

  output.table = "output.table.name"
}
